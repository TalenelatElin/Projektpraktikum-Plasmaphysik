// Hier sind die Informationen über das Ausmaß/Form der Simulation gespeichert
// Hier können nur die Werte geändert werden!


// Name der Datei die gedownloaded wird (Keine Anführungszeichen!)
dateiname=Diskrete_Braunsche_Bewegung9.txt

// Anzahl der Rekursionsschritte
sim_len=10000

// Anzahl der nötigen Anfangszustände bzw der Anzahl der vorherigen Zustände, die nötig sind um den nächsten auszurechnen (Rekursion!)
depth=1

// Differentialgleichungsschritt
h=0.1

// Anzahl der (screen)Updates die während der Simulation durchgeführt werden
nr_saves=100000
