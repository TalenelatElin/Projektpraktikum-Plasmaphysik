#pragma once

// Hier werden systemtypische Konstanten definiert.
// Das Programm erkennt somit diese Variablen, falls sie in anderen config dateien verwendet werden.

inline constexpr int N = 10;
inline constexpr float q = -1;
inline constexpr int dx = 1;
inline constexpr int dy = 1;
inline constexpr int dz = 1;